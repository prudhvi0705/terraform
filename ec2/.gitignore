# Terraform-generated directories
.terraform/
**/.terraform/

# Terraform state files (may contain sensitive data)
*.tfstate
*.tfstate.*

# Terraform crash log files
crash.log

# Terraform override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan files (often contain sensitive data)
*.tfplan

# Terraform variable files (if containing sensitive data not to be committed)
*.tfvars
*.tfvars.json

# Local Terraform CLI configuration files
.terraformrc
terraform.rc